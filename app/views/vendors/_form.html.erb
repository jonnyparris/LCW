<%= simple_form_for(@vendor) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :contact_details %>
    <%= f.input :website %>
    <%= f.input :description %>
    <%= f.input :pricing %>
    <%= f.input :services %>
    <%= f.input :photos, as: :file, input_html: { multiple: true } %>
    <%= f.association :preferred_venues, as: :check_boxes %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
